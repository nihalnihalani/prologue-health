.DS_Store
node_modules/
.env
.env.local
.next/
dist/
*.log
.vercel

tsconfig.tsbuildinfo

# Large local media working directory (video renders, VO, mixes).
# These are hundreds of MB of binaries and must never enter git history.
video/
*.mp3
*.mp4
*.wav
